const css = require('sheetify')

const style = css`
  :host {
    color: var(--w);
    font-family: 'HKG', sans-serif;
    font-size: 15px;
    font-weight: normal;
    justify-content: center;
    margin-top: -1px;
  }
`

module.exports = style
