import css from 'csjs-inject'

const style = css`
  .base {
    margin-top: 2px;
    margin-left: 0;
    padding-left: 0;
  }
  .label {
    color: currentColor;
    font-family: 'Mono';
    font-size: 0.75rem;
    margin-top: 0px;
  }
`
export default style
