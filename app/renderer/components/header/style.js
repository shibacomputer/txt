import css from 'csjs-inject'

const style = css`
  .header {
    align-items: center;
    color: var(--text);
    display: flex;
    flex-direction: row;
    height: 2rem;
    justify-content: center;
    width: 100%;
  }
  .title {
    color: var(--text);
    font-family: 'Mono';
    font-weight: normal;
    font-size: 12px;
  }

`

export default style
